//CommonJS, every file is module (by default)
// module - Encapulated code  (only share minmum)

const names = require('./4-names')
const sayHi = require('./5-utils')
const data = require('./6-alternative')
require('./7-mind-grenade')

// console.log(data);
// sayHi('Mohamed')
// sayHi(names.john)
// sayHi(names.peter)
